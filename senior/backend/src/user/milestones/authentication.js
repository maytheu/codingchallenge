'use strict';

// const {
//   Errors: { BadRequestError },
// } = require('finale-rest');

// const {
//   API: {
//     HEADERS: { X_SLUG },
//     SLUGS: { MYSELF },
//   },
// } = require('../../config');

module.exports = (req, res, context) => {
  return context.continue;
};
